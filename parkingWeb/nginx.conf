server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html;
        # Esto es CLAVE: si no encuentra la ruta, carga el index.html
        try_files $uri $uri/ /index.html;
    }
}
